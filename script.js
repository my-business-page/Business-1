const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);window.addEventListener('load',()=>setTimeout(()=>$('.loader')?.classList.add('done'),650));document.addEventListener('DOMContentLoaded',()=>{const header=$('.site-header'),toggle=$('.menu-toggle'),nav=$('.nav');window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>40),{passive:true});toggle?.addEventListener('click',()=>nav?.classList.toggle('open'));const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting?e.target.classList.add('visible'):e.target.classList.remove('visible')),{threshold:.12});$$('.reveal').forEach(e=>io.observe(e));const c=$('#vfx'),x=c?.getContext('2d');if(x){let pts=[];function resize(){c.width=innerWidth;c.height=innerHeight;pts=Array.from({length:55},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.5+.3,v:Math.random()*.25+.05}))}resize();addEventListener('resize',resize);function draw(){x.clearRect(0,0,innerWidth,innerHeight);pts.forEach(p=>{p.y-=p.v;if(p.y<0)p.y=innerHeight;x.beginPath();x.arc(p.x,p.y,p.r,0,Math.PI*2);x.fillStyle='rgba(233,228,216,.45)';x.fill()});requestAnimationFrame(draw)}draw()}const y=$('#year');if(y)y.textContent=new Date().getFullYear()});